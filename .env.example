# adaptive-rate-limiter Environment Configuration
# Copy this file to .env and customize for your environment
# All variables are optional unless marked [REQUIRED]

# ============================================================================
# Redis Configuration
# ============================================================================

# Redis connection URL (optional)
# Default: redis://localhost:6379
# Format: redis://[username:password@]host[:port][/db_number]
# REDIS_URL=redis://localhost:6379

# Redis Cluster connection URL (optional)
# Used when cluster_mode=True on RedisBackend
# Format: redis://[username:password@]host[:port]
# REDIS_CLUSTER_URL=redis://redis-cluster:7000

# ============================================================================
# Development Settings (for contributors)
# ============================================================================

# Skip Redis cluster integration tests (optional)
# Tests are automatically skipped if REDIS_CLUSTER_URL is not set or
# the cluster is not reachable. This variable is not directly read by tests,
# but you can control cluster test execution by setting/unsetting REDIS_CLUSTER_URL.
#
# To run cluster tests locally, start the cluster with:
#   docker-compose -f docker-compose.redis-cluster.yml up -d
# Then set:
# REDIS_CLUSTER_URL=redis://localhost:7001
